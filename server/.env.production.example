# =============================================================================
# Invoice Baba — Production Environment Variables
# =============================================================================
# Copy this file to .env and fill in ALL values before deploying.
# The server will refuse to start if required variables are missing.
# =============================================================================

NODE_ENV=production
PORT=3000
LOG_LEVEL=info

# --- Database (REQUIRED) ---
# Use connection pooling (e.g. PgBouncer) in production.
# Append ?connection_limit=10&pool_timeout=30 for Prisma pool tuning.
DATABASE_URL="postgresql://user:password@db-host:5432/invoice_baba?schema=public&connection_limit=10"

# --- CORS (REQUIRED in production) ---
# Comma-separated list of allowed origins. Must NOT be localhost.
CORS_ORIGIN=https://app.invoicebaba.com

# --- JWT (REQUIRED — minimum 32 characters in production) ---
JWT_SECRET="generate-a-strong-random-secret-at-least-32-characters-long"
JWT_EXPIRES_IN=7d

# --- OTP Configuration ---
OTP_EXPIRY_MINUTES=5
OTP_LENGTH=6

# --- SMS Provider (REQUIRED for OTP in production) ---
SPRING_EDGE_API_KEY_ID=your-springedge-api-key
SPRING_EDGE_SENDER=INVBBA

# --- Razorpay (REQUIRED for paid plan subscriptions) ---
RAZORPAY_KEY_ID=rzp_live_xxxxxxxxxxxxx
RAZORPAY_KEY_SECRET=your-razorpay-live-key-secret

# --- Google Cloud Storage (REQUIRED for logo/signature uploads) ---
# Option 1: Full service-account JSON key (recommended)
GOOGLE_CLOUD_SERVICE_ACCOUNT_KEY='{"type":"service_account","project_id":"...","client_email":"...","private_key":"..."}'
# Option 2: Individual fields
# GCS_CLIENT_EMAIL=sa@project.iam.gserviceaccount.com
# GCS_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"
GOOGLE_PROJECT_ID=your-gcp-project-id
GCS_BUCKET=invoicebaba-uploads
